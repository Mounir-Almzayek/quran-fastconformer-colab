# Google Colab dependencies for the FastConformer Quran ASR experiment.
# Colab supplies CUDA Torch and several runtime packages; avoid upgrading them broadly.
# NeMo's current audio preprocessing path still requires NumPy's pre-2.0 np.sctypes API.
numpy==1.26.4
nemo_toolkit[asr]>=2.0,<3.0
datasets>=3.2,<4.0
soundfile>=0.12,<0.14
jiwer>=3.0,<4.0
pyyaml>=6.0,<7.0
pandas==2.2.2
fsspec==2025.3.0
matplotlib>=3.7,<4.0
